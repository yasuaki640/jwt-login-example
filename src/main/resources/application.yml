debug : true
spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

